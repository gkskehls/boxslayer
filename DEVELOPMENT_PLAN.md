# BoxSlayer 개발 계획 (Development Plan)

이 문서는 'BoxSlayer' 프로젝트의 개발 순서와 각 단계별 목표를 정의합니다. 실제 구현에 앞서 전체적인 개발 로드맵을 공유하고, 효율적인 진행을 위한 가이드라인으로 활용됩니다.

## Phase 1: 핵심 게임 플레이 및 기본 UI 완성 (완료)

**목표:** 게임의 기본적인 흐름(타이틀 -> 로그인 -> 메인 허브/전투)을 확립하고, 플레이어가 게임을 시작하고 스탯을 강화하며 전투를 경험할 수 있도록 합니다. 모든 인터페이스는 텍스트 기반의 단순한 형태로 구현합니다.

### [x] 1.1. 초기 화면 흐름 및 사용자 인증 (완료)
* **목표:** 사용자가 게임에 처음 접속했을 때의 경험을 정의하고, 기본적인 로그인 방식을 제공합니다.
* **주요 작업:**
  * [x] `App.tsx`에 `TITLE_SCREEN`, `LOGIN_CHOICE_SCREEN`, `MAIN_HUB_SCREEN` 상태 추가.
  * [x] 각 화면에 맞는 텍스트 기반 UI 구현.
  * [x] "게스트 로그인" 선택 시 `MAIN_HUB_SCREEN`으로 전환 로직 구현.
  * [x] 게임 오버 시 `resetGame` 호출 및 `MAIN_HUB_SCREEN`으로 복귀 로직 구현.

### [x] 1.2. 스탯 밸런싱 초기 적용 및 UI 개선 (완료)
* **목표:** `GAME_DESIGN.md`에 정의된 스탯 밸런싱 가이드라인을 `gameStore`에 반영하고, 스탯 강화 UI의 피드백을 개선합니다.
* **주요 작업:**
  * [x] `gameStore.ts`의 `distributeStat` 로직을 업데이트하여 각 스탯 투자 시 증가량 (`공격력 +1`, `방어력 +1`, `최대체력 +10`, `공격속도 +0.05`)을 `GAME_DESIGN.md`에 맞춰 정확히 적용.
  * [x] 스탯 강화 버튼 클릭 시, 해당 스탯이 얼마나 증가하는지 텍스트로 명확하게 표시하는 UI 개선 (예: 버튼 텍스트에 `+1 ATK` 등).

### [x] 1.3. 사용자 데이터 관리 (게스트 로그인) (완료)
* **목표:** 게스트 사용자의 게임 진행 데이터를 로컬 스토리지에 저장하고 불러오는 기능을 구현하여, 브라우저를 닫았다 열어도 진행 상황이 유지되도록 합니다. **(참고: 이 기능은 데이터베이스 준비 전까지 임시적으로 로컬 스토리지를 활용하며, 추후 Firebase 등 실제 데이터베이스 연동으로 전환될 예정입니다.)**
* **주요 작업:**
  * [x] `gameStore`에 `saveGame` 및 `loadGame` 액션 추가.
  * [x] `localStorage`를 사용하여 `GameState`를 JSON 형태로 저장/불러오기.
  * [x] 게임 시작 시 (또는 로그인 후) 저장된 데이터가 있으면 불러오고, 없으면 `initialPlayer`로 시작하는 로직 구현.
  * [x] `gameStore`의 상태 변화 시 자동으로 `saveGame`이 호출되도록 `useEffect` 또는 `subscribe` 활용.

### [x] 1.4. 화면 내비게이션 시스템 구현 (완료)
* **목표:** 게임의 다양한 화면(마을, 전투, 스탯, 코어, 상점 등) 간의 전환을 관리하는 시스템을 구축하고, `App.tsx`의 역할을 화면 라우팅 및 레이아웃 관리로 분리합니다.
* **주요 작업:**
  * [x] `App.tsx`의 `GameScreen` 타입을 확장하여 `TOWN_SCREEN`, `BATTLE_SCREEN`, `STATS_SCREEN`, `CORE_SCREEN`, `SHOP_SCREEN` 등 추가.
  * [x] `App.tsx` 내에서 `screen` 상태에 따라 각 화면 컴포넌트를 조건부 렌더링하도록 구조 변경.
  * [x] `TOWN_SCREEN` (마을 화면) 컴포넌트의 기본 레이아웃 및 내비게이션 버튼 (전투, 스탯, 코어, 상점) 구현.
  * [x] 기존 `MAIN_HUB_SCREEN`의 전투 관련 UI를 `BATTLE_SCREEN` 컴포넌트로 분리.
  * [x] 기존 `MAIN_HUB_SCREEN`의 스탯 강화 UI를 `STATS_SCREEN` 컴포넌트로 분리.
  * [x] 각 화면 컴포넌트 간 전환을 위한 액션 (`goToTown`, `goToBattle`, `goToStats` 등) 구현.

### [x] 1.5. UI/UX 개선 (초기 단계) (완료)
* **목표:** 현재 텍스트 기반의 UI에 시각적인 구분을 추가하고, 스탯 관리 화면의 사용성을 개선하며, 전역 내비게이션을 도입하여 사용자 경험을 향상시킵니다.
* **주요 작업:**
  * [x] **UI 영역 구분:** 각 화면(전투, 스탯, 코어 등) 내에서 주요 섹션들을 시각적으로 구분.
  * [x] **남은 스탯 포인트 표시:** `StatsScreen` 컴포넌트 내에 현재 플레이어의 `statPoints`를 명확하게 표시.
  * [x] **스탯 버튼 명확화:** `StatsScreen`의 스탯 증가 버튼이 클릭 가능한 버튼처럼 보이도록 스타일 개선.
  * [x] **글로벌 내비게이션 바:** 모든 게임 화면 하단에 고정된 내비게이션 바를 구현하여 화면 간 직접 이동 지원.

## Phase 2: 코어 시스템 개편 (완료)

**목표:** 코어 인벤토리를 배제하고, 환생 후 4대 원소 중 단일 코어를 선택하여 육성하는 직관적인 빌드 시스템을 구축합니다.

### [x] 2.1. 코어 데이터 구조 정의 (완료)
* **목표:** `GAME_DESIGN.md`에 정의된 코어의 속성들을 TypeScript 타입으로 정의합니다.
* **주요 작업:**
  * [x] `src/types/game.ts`에 `CoreType`, `Core` 인터페이스 정의 (`id`, `name`, `type`, `level`).
  * [x] 인벤토리 구조를 배제하고 `equippedCore: Core | null` 단일 모델 적용.

### [x] 2.2. `gameStore`에 코어 로직 추가 (완료)
* **목표:** `gameStore`가 단일 코어 선택 및 골드 레벨업을 처리할 수 있도록 구현합니다.
* **주요 작업:**
  * [x] `GameState`에 `equippedCore: Core | null` 관리.
  * [x] `selectCore(type)` 액션으로 회차별 단일 코어 선택.
  * [x] `upgradeCore(amount)` 액션으로 골드 소모 레벨업 및 Guard Clause 적용.
  * [x] `reincarnate()` 액션 시 코어 선택 리셋 처리.

### [x] 2.3. 코어 관리 UI 구현 (완료)
* **목표:** 미선택 시 4대 원소 선택 카드 UI, 선택 완료 시 활성 코어 정보 및 +1/+10/+100/MAX 레벨업 UI 제공.
* **주요 작업:**
  * [x] `CoreScreen.tsx` 조건부 렌더링 (미선택: 4대 원소 카드 선택 뷰 / 선택: 활성 코어 상세 & 골드 강화 패널).
  * [x] 실시간 `getCoreStats` 함수를 통한 패시브 스킬 보너스 연동 효과 표출.
  * [x] 최대 강화(MAX) 계산 공식 연동.

### [x] 2.5. 전투 로직에 코어 효과 반영 (완료)
* **목표:** 장착된 코어의 특수 효과가 실제 전투에 적용되도록 `gameStore`의 전투 로직을 수정합니다.
* **주요 작업:**
  * [x] `attackEnemy` 및 `attackPlayer` 액션 내에서 장착된 코어 타입을 확인하여 특수 효과 연동.
  * [x] **불 코어:** 방어력을 100% 무시하는 화염 트루 피해 엔진 구축.
  * [x] **물 코어:** 매 층 리셋되는 시작 대량 쉴드 및 타격 재생, 피해 반사 메커니즘 연동.
  * [x] **바람 코어:** 누적 타격 수 기반의 확정 연격 및 확정 회피 잔상 차단 로직 구현.
  * [x] **번개 코어:** 누적 타격 수 기반의 적 기절 제어 및 무방비 상태 적 대상 트루 데미지(처형) 로직 구현.

## Phase 3: 게임 진행 및 메타 시스템 확장 (현재 진행 중)

**목표:** 게임의 진행 단계를 구체화하고, 플레이어가 장기적으로 게임을 즐길 수 있는 메타 시스템의 기반을 마련합니다.

### [x] 3.1. 시스템 안정성 및 예외 처리 강화 (완료)
* **목표:** 데이터 무결성 강화 및 타입 안정성 확보.
* **주요 작업:**
  * [x] `upgradeCore` 로직 내 골드 부족 방지 Guard Clause 구현.
  * [x] 골드 마이너스 오류 차단 및 사용자 알림(`alert`) 로직 추가.
  * [x] ESLint 경고 해결 및 타입 안정성 최적화.

### [x] 3.2. 적 박스 연속 성장 및 원킬 차원 도약 시스템 (완료)
* **목표:** 보스 병목을 배제한 연속형 차원 박스 성장 및 초반/환생 지루함 타파를 위한 원킬 차원 도약(One-Shot Warp) 구현.
* **주요 작업:**
  * [x] `gameStore.ts`의 `spawnEnemy` 로직에서 보스 인위적 배율(HP/ATK 급증) 제거 및 단일 지수 스케일링 적용.
  * [x] 몬스터 1타 처치(원킬) 시 +3층 즉시 차원 도약 및 비례 보상 일괄 수급 로직 구축.
  * [x] 상점 내 불필요한 보스 추적기 아이템 정리.
  * [x] `GameState`에 `gold: number` 필드 추가 및 `attackEnemy` 액션에서 골드 지급 처리.
  * [x] UI에 현재 골드 보유량 표시.

### [x] 3.3. 환생 시스템 및 패시브 스킬 트리 구현 (완료)
* **목표:** 환생 로직(포인트 계산 및 초기화)과 PoE 스타일의 스킬 트리 UI/엔진을 구현합니다.
* **주요 작업:**
  * [x] `GameState`에 `reincarnationPoints: number` 및 `acquiredNodes: string[]` (활성화된 노드 ID 리스트) 필드 추가.
  * [x] `reincarnate()` 액션 구현:
    * [x] 오직 진행 층수(Stage)에만 기반한 지수형 환생 포인트(RP) 보상 산출 엔진 적용.
    * [x] 플레이어 상태(스테이지, 레벨, 스탯, 골드, 코어 레벨) 초기화.
    * [x] `reincarnationPoints` 누적 및 `acquiredNodes` 유지.
  * [x] `SkillTree` 데이터 정의:
    * [x] 모든 노드 정보(ID, 능력치, 연결 정보, 위치 좌표)를 담은 정적 데이터 생성.
  * [x] `TownScreen` 내 "환생 및 스킬 트리" 화면 구현:
    * [x] 환생 버튼: 현재 진행 상황 기준 획득 예상 포인트 표시.
    * [x] 트리 UI: SVG를 활용하여 노드와 연결 선을 렌더링.
    * [x] 노드 클릭 시: 포인트 소모 및 인접 노드 확인 후 활성화 로직.
  * [x] `getComputedStats` 고도화:
    * [x] 현재 활성화된 `acquiredNodes`를 기반으로 보너스 수치를 실시간 합산하여 실제 플레이어 스탯에 적용.

### [x] 3.4. 오프라인 보상 시스템 및 전투 UI/UX 통합 리뉴얼 (완료)
* **목표:** 접속하지 않은 시간 동안 재화를 획득하는 오프라인 보상 시스템 및 전투 화면 연출/HUD 통합 리뉴얼.
* **주요 작업:**
  * [x] `GameState`에 `lastOnlineTime: number` 추가.
  * [x] `calculateOfflineRewards()` 액션을 통한 시간당 골드/경험치 산출 로직 적용.
  * [x] 접속 시 오프라인 보상 정산 UI 안내.
  * [x] **전투 화면 HUD & 연출 리뉴얼**:
    * [x] 레트로 아케이드 픽셀 HP 바 & 쉴드(Cyan) 덧띠 오버레이 (`RetroHpBar`) 통합.
    * [x] Player / Enemy Status Window (속성 코어 뱃지, 현재/최대 HP, 쉴드 실시간 표출) 가독성 극대화.
    * [x] 피버 타임 배속 단계(1.5x / 5.0x / 10.0x / 50.0x)별 시각 모션 타이머(visualInterval) 및 데미지 팝업 간격/유지시간(popupDuration) 가독성 최적화.
    * [x] 연격/코어 속성 데미지 팝업 및 전투 로그 정돈.

### [x] 3.6. 재화(코어 조각/박스 조각) 실제 전투 드랍 밸런싱 & 오프라인 보상 분리 (완료)
* **목표:** 코어 조각(💎) 및 박스 조각(📦)의 오프라인 보상 지급을 제한하고, 실제 전투 승리 시에만 100회 전투당 약 1개(기본 1.0%)의 절제된 확률로 드랍되도록 밸런싱.
* **주요 작업:**
  * [x] **오프라인 보상 분리**: 오프라인 방치 보상에서는 골드 및 경험치만 지급되며 조각 재화는 일체 미지급.
  * [x] **실제 전투 드랍율 산출**: 기본 1.0% 드랍율(전투 100회당 약 1개), 원샷 3층 도약 등반 시 약 300층 통과 시점당 1개 체감 밸런스 적용.
  * [x] **전투 화면 재화 바 연동**: 상단 헤더에 골드(🪙), 코어 조각(💎), 박스 조각(📦) 보유량 실시간 표출.

### [x] 3.7. HUD 통합, 코어 강화 편의성 및 간략 전투 로그 고도화 (완료)
* **목표:** 정보 전달력을 극대화한 컴팩트 상단 HUD, 코어 4단계 레벨업 인터페이스, 정렬된 타이머 및 직관적 간략 전투 로그 구축.
* **주요 작업:**
  * [x] **코어 강화 버튼 개편:** +1, +10, +100, MAX 4단계 버튼 제공 및 버튼 내 강화량/소모 골드 직접 표기.
  * [x] **통합 상단 HUD:** 1행(스테이지/레벨/얇은 경험치 바/스탯 버튼) + 2행(골드/코어/상자/방치 버튼 4분할 일렬 배치).
  * [x] **스탯 및 보상 버튼 규격 일치:** 방치 버튼과 스탯 버튼의 형태, 너비, 여백 통일.
  * [x] **타이머 높이 정렬:** 플레이어/적 코어 표시 텍스트와 동일한 상단 중앙 높이에 은은한 워터마크 타이머 정렬.
  * [x] **체력바 & 쉴드 일체형 연출:** 전투 영역 최상단에 영역 분리 없는 네이티브 일체형 체력바 오버레이.
  * [x] **간략 전투 로그:** `[나 ➜ 적] 일반 {수치} | {속성} {수치} (🛡️{흡수량} 흡수) ⚡{연격}` 등 모든 핵심 정보를 대상 구분과 함께 컴팩트 표기.
  * [x] **패배 시 10층 전 이동:** 전투 패배 시 현재 층에서 10층 이전으로 안전 퇴각 로직 구현.

### [x] 3.8. 층수 기반 적 코어 티어 해금 시스템 (완료)
* **목표:** 코어 조각 획득 속도(100층당 1개)와 적 코어 능력 개방을 완벽히 동기화하여 초반 난이도 붕괴 방지 및 단계별 등반 긴장감 부여.
* **주요 작업:**
  * [x] **스테이지별 적 코어 티어 함수 구축 (`getEnemyCoreTier`):** 1~99층(Tier 0: 0연구 기본 코어), 100층+(Tier 1), 1,000층+(Tier 2), 10,000층+(Tier 3), 100,000층+(Tier 4), 1,000,000층+(Tier 5).
  * [x] **적 코어 능력 단계적 개방:** 1~99층 기초 속성 피해/기본 쉴드 적용, 100층 이상부터 기초 속성 피해 및 시작 쉴드 강화, 1,000층부터 비로소 감전 기절(⚡), 타격 쉴드 충전(💧), 질풍 연격(🍃) 적용.
  * [x] **전투 HUD 티어 뱃지 표기:** 적의 해금 단계에 맞춘 코어 티어 라벨 표출 (`🔥불 T0`, `⚡전기 T1` 등).

### [x] 3.9. 공속 고정 무결성 보장 및 전투 로그 층·턴수 로테이션 뱃지 시각화 (완료)
* **목표:** 공속 1.0/초 고정 원칙 준수 및 전투 로그에 실시간 스테이지와 턴 수(`[1F · T1]`)를 층별·턴별 색상 로테이션 뱃지로 표기하여 빠른 전투 로그의 직관적 시인성 확보.
* **주요 작업:**
  * [x] **스탯 연산 공속 1.0 고정:** `getComputedStats`의 `attackSpeed`를 1.0으로 고정하여 플레이어와 적의 공격 주기를 1초 단위로 완전히 동기화.
  * [x] **동일 층수 동일 색상 뱃지 (6종 순환):** 스테이지 번호에 따라 Amber, Sky, Emerald, Purple, Rose, Teal 배경 및 텍스트 뱃지 자동 순환 적용.
  * [x] **동일 턴수 동일 색상 폰트 (6종 순환):** 턴 번호에 따라 Yellow, Cyan, Lime, Fuchsia, Orange, Violet 고대비 폰트 자동 순환 적용.
  * [x] **전투 액션 그룹화 시인성 극대화:** 한 턴 내에서 일어나는 일반 공격, 속성 피해, 쉴드, 흡혈 등의 이벤트가 동일한 층·턴 뱃지를 공유하여 한눈에 턴 단위 파악 가능.

### [x] 3.10. 전투 일시정지(Pause) 및 로그 확장 검토 시스템 (완료)
* **목표:** 전투 시뮬레이션 일시정지 및 로그 확장 토글을 제공하여 턴별 상세 수치와 전투 흐름을 정밀하게 분석할 수 있는 환경 제공.
* **주요 작업:**
  * [x] **전투 일시정지(Pause) 제어:** 전투 30초 타이머, 피버 타이머, 공격 루프, 몬스터 스폰을 완벽히 동결시키는 `isPaused` 상태 구현.
  * [x] **아레나 정지 인디케이터:** 일시정지 시 화면 중앙에 `[⏸️ 전투 일시정지됨]` 안내 뱃지 및 원클릭 재개 버튼 제공.
  * [x] **전투 로그창 2배 확장 토글:** `[확대 🔍]` 버튼을 통해 이전 20개 이상의 턴별 상세 전투 로그를 여유롭게 스크롤 검토 가능.


## Phase 4: PvP 시스템 및 게임 확장 (추후 확장)

**목표:** 기존 4개 스탯 체계를 3대 스탯으로 리팩토링하고, 플레이어 간 경쟁 요소를 도입하며 게임의 몰입도를 높입니다.

### [x] 4.1. 스탯 체계 리팩토링 (완료)
* **목표:** 일관성 있는 스탯 관리를 위한 3대 스탯 체계 전환.
* **주요 작업:**
  * [x] `Stats` 타입 정의를 3대 스탯(STR, DEX, CON)으로 변경.
  * [x] `gameStore` 내 스탯 분배 로직을 3대 스탯 기반으로 전면 수정.
  * [x] 스탯 총합 및 비율에 따른 외형(색상/크기) 계산 유틸리티 함수 구현 (`BattleScreen` 동적 스타일링 적용).
  * [x] 모든 스탯 변경 및 전투 계산 시 실시간 계산 패턴(`getComputedStats`) 적용.

### [ ] 4.2. PvP 빌드 및 대전 시스템 구축
* **목표:** 유저 간 경쟁을 위한 비동기 1:1 대전 기능 구현.
* **주요 작업:**
  * [ ] PvP 빌드 저장을 위한 `PvPProfile` 상태 추가.
  * [ ] 1:1 대전 엔진 (`combatEngine.ts`) 분리 구현 (제한 시간, 판정, 회피 확률 등).
  * [ ] 메인 허브 화면에 "PvP 대전" 버튼 및 빌드 설정 UI 구현.
  * [ ] 상대방 빌드 정보 식별을 위한 박스 시각화 UI 컴포넌트 고도화.

### [ ] 4.3. 사운드 및 시스템 설정
* **목표:** 게임의 타격감과 몰입도를 극대화할 사운드 시스템 추가.
* **주요 작업:**
  * [ ] 배경 음악(BGM) 및 코어 속성별 전투 타격음(SFX) 에셋 연동.
  * [ ] 설정 화면에 사운드 On/Off, 볼륨 조절 UI 구현.

### [ ] 4.4. 대형 스킬 트리 및 연격 시스템 구현 [구현 지향]
* **목표:** 게임의 6개월 장기 파밍을 지탱할 대형 500개 스킬 트리 엔진 및 연격 시스템 구축.
* **주요 작업:**
  * [ ] `SKILL_TREE_BALANCING_PLAN.md`에 명세된 고정+백분율 하이브리드 노드 타입 데이터 정립.
  * [ ] 누적 타격형 / 확률형 연격(Multi-Hit) 전투 엔진 공식 코딩 및 피버 모드와의 연동.
  * [ ] 500개 대형 노드를 소화하기 위한 줌/드래그 가능 SVG 패시브 캔버스 뷰포트 컬링 렌더러 제작.
  * [ ] 기하급수적 성장 곡선에 맞춘 지수함수형 RP(환생 포인트) 요구량 및 해금 데이터 적용.
