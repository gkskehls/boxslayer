# 🌌 BoxSlayer - 대규모 스킬 트리 및 장기 밸런싱 로드맵
본 문서는 'BoxSlayer'의 장기 서비스(6개월 이상)를 목적으로 설계된 대규모 패시브 스킬 트리 시스템(코어 루트당 100개 노드, 총 500개 노드 규모)의 수학적 밸런싱과 시스템 아키텍처 설계 명세서입니다.

---

## 1. 핵심 설계 철학 (Core Philosophy)

### ① 초반 고정 스탯(Flat) ➡️ 후반 백분율(%)의 완벽한 징검다리
* **초반 (1~3티어 노드)**: 고정 스탯(예: `힘 +10`, `체력 +50`)을 배치하여 환생 직후 1레벨 단계에서 기하급수적인 속도의 초반 고속 등반(파워 판타지)을 지원합니다.
* **후반 (4~10티어 노드)**: 기본 스탯 및 장비가 성장함에 따라 백분율(예: `힘 +3%`, `총 체력 +5%`)이 곱연산 형태로 적용되도록 하여 후반부 한계 도달 시점에서의 스킬 밸류를 무한히 보존합니다.

### ② 공격 속도 절대 고정 및 연격(Multi-hit) 대체
* **배경**: 공격 속도(ASPD)는 스테이지 클리어 제한 시간(Time-out 기믹) 및 물리 엔진 주기와 직접적으로 동기화되어 있어 민감하게 조작할 경우 프레임 드롭이나 밸런스 붕괴를 유발합니다. 따라서 **공격 속도는 완벽히 고정**합니다.
* **대체재 (연격 시스템)**: 공속을 올리는 대신, 일정 확률 또는 누적 타격 수 기반으로 **연격(Multi-hit / 2연타, 3연타)**을 추가하여 시각적 타격감과 초당 타격 횟수(DPS) 증가를 동시에 만족시킵니다.

### ③ 장기 라이프사이클 (6개월 도달 플랜)
* **3차 코어 최종 해금(종결 컨텐츠)**을 최소 **6개월**간의 매일 꾸준한 플레이(또는 수백 번의 유의미한 환생) 이후 도달하는 신성한 영역으로 설정합니다.
* 해금 속도 역시 초반부의 시원시원함에서 후반부의 극단적인 희소성으로 서서히 조여오는 **지수함수적 곡선**을 그립니다.

---

## 2. 연격(Multi-Hit / Combo) 시스템 설계 명세

스킬 트리를 통해 획득하는 연격 수치는 플레이어의 기본 공격 주기를 쪼개어 다단 히트를 발생시킵니다.

### ① 연격 발동 공식
* **누적형 연격 (바람/번개 특화)**: 매 타격 시 연격 스택이 쌓이며, 스택이 $N$회 도달 시 다음 공격이 추가 계수(M%)만큼 2회 연속 타격 처리됩니다.
* **확률형 연격 (공통)**: 스킬 노드에서 해금한 `연격 확률(%)`에 따라 매 공격 시 $P\%$의 확률로 즉시 연격을 가합니다.
  $$\text{연격 데미지} = \text{기본 데미지} \times \text{연격 배율(%)}$$

### ② 피버 모드(Fever) 연동
* 피버 모드 진입 시, 연격 확률이 2배로 증가하거나 연격 시 발생하는 타격 수가 $+1$회 가산(2연격 ➡️ 3연격)되어 폭발적인 극딜 타임을 제공합니다.

---

## 3. 대규모 스킬 트리 아키텍처 (500개 노드)

스킬 트리는 총 5개의 지엽 루트로 갈라지며, 루트당 약 100개의 노드가 방사형 혹은 트리 형태로 연결됩니다.

```
                  [ Core Origin (기원의 상자) ]
                               │
       ┌───────────┬───────────┼───────────┬───────────┐
       ▼           ▼           ▼           ▼           ▼
   [불의 코어] [물의 코어] [바람의 코어] [번개의 코어] [차원 유틸]
   (100개 노드) (100개 노드) (100개 노드) (100개 노드) (100개 노드)
```

### 루트별 고유 특색 명세
1. **불의 루트 (FIRE - 100개)**: 힘(STR) 고정/비율 상승, 화상 확률 증가, 화상 도트 주기 단축, 방어 관통 배율 업그레이드.
2. **물의 루트 (WATER - 100개)**: 체력(CON) 고정/비율 상승, 시작 쉴드 최대치 확장, 반사 데미지 배율 증가, 피격 시 체력 즉시 재생.
3. **바람의 루트 (WIND - 100개)**: 민첩(DEX) 고정/비율 상승, **연격 확률 증가**, **연격 배율 강화**, 잔상(회피) 스택 상한 증가.
4. **번개의 루트 (ELECTRIC - 100개)**: 전체 균형 스탯 상승, 기절 확률 및 지속시간 증가, 기절 대상 처형(치명타) 추가 데미지 비율 고도화.
5. **차원 유틸 루트 (UTIL - 100개)**: 오프라인 보상 정산 배율 증가, 환생 포인트(RP) 보너스 배율 증가, 상점 아이템 할인율, 시작 스테이지 가산.

---

## 4. 환생 주기 및 스킬 해금 타임라인 설계 (수학적 밸런싱)

플레이어가 게임을 진행하면서 마주하는 환생 주기별 노드 해금 가격 곡선은 다음과 같이 설계됩니다. 후반부로 갈수록 스킬 노드의 비용은 기하급수적으로 폭증하며, 환생당 획득하는 RP 수급량 역시 스테이지 정체 구간에서 수렴하게 만듭니다.

### ① 성장 페이즈별 밸런싱 곡선

| 페이즈 | 플레이 기간 | 누적 환생 횟수 | 1회 환생당 RP 수급 | 타겟 노드 등급 | 해금 주기 (환생당 스킬 수) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **극초반 (Phase 1)** | 1일 ~ 3일 | 1 ~ 5회 | 1 ~ 5 RP | 1티어 일반 (Flat 스탯) | **환생 1회당 1~2개 해금** (비용: 1~2 RP) |
| **초중반 (Phase 2)** | 4일 ~ 2주 | 6 ~ 20회 | 10 ~ 30 RP | 2~3티어 일반, 1차 Notable | **환생 1회당 1개 해금** (비용: 10~25 RP) |
| **중반 (Phase 3)** | 3주 ~ 2개월 | 21 ~ 100회 | 50 ~ 200 RP | 4~6티어 (% 스탯), 2차 Notable | **2~3회 환생당 1개 해금** (비용: 300~500 RP) |
| **후반 (Phase 4)** | 3개월 ~ 5개월 | 101 ~ 300회 | 300 ~ 800 RP | 7~9티어 (% 유틸), 3차 Notable | **10~20회 환생당 1개 해금** (비용: 5,000~15,000 RP) |
| **극후반 (Phase 5)** | **6개월 이상** | **300회 초과** | 1,000 ~ 2,000 RP | 10티어 Keystone (최종 해금) | **100회 환생당 1개 해금** (비용: 150,000~200,000 RP) |

### ② 6개월 도달 타임라인의 수학적 검증

#### [가정]
* 유저가 하루에 2~3회 집중해서 고층 환생을 진행한다고 가정 (한 달 누적 환생 약 70~80회, 6개월 기준 약 450~500회 환생).
* 환생 시 획득하는 RP 공식:
  $$\text{Earned RP} = \text{Stage Point} + \text{Level Point} + \text{Core Point}$$
  * 스테이지 1000층 돌파 시: `Stage Point = 200`
  * 레벨 500 달성 시: `Level Point = 50`
  * 코어 레벨 합이 200일 때: `Core Point = 20`
  * 즉, 후반 하이엔드 유저의 1회 환생 수급량은 약 **270 RP** 내외로 수렴하게 됩니다. (보스 트랙 등 상점 버프와 스킬 유틸 보너스를 전부 감안해도 최대 **500~1,000 RP** 수준으로 통제).

#### [비용 설정]
* 3차 코어 해금(최종 10티어 Keystone) 노드의 비용을 **150,000 RP ~ 200,000 RP**로 설정합니다.
* 유저가 하루 평균 1,500 RP를 파밍한다고 했을 때, 최종 노드 1개를 뚫는 데 순수하게 **100일 ~ 130일(약 3~4개월)** 동안 한 푼도 쓰지 않고 모아야 하는 초장기 설계가 성립됩니다.
* 4개의 코어 최종 노드를 모두 마스터하는 데는 완벽히 6개월에서 1년 이상의 주기적 컨텐츠 수명을 확보할 수 있습니다.

---

## 5. 기술적 개발 구현 가이드라인 (Dev Note)

루트당 100개, 총 500개의 노드를 단순 SVG 단일 컴포넌트에 올리면 화면 렉(Lag) 및 웹 브라우저의 렌더링 한계가 올 수 있습니다. 이를 부드럽게 구현하기 위한 기술적 대비책입니다.

1. **무한 줌 & 드래그 캔버스 (Zoom-and-Pan Canvas)**:
   * 스킬 트리 화면에 `framer-motion`의 `drag` 기능과 SVG scale 매트릭스를 결합하여, 유저가 마우스 드래그 및 휠로 거대한 별자리 같은 500개 노드 트리를 부드럽게 탐험할 수 있도록 전용 뷰포트 엔진을 구축합니다.
2. **뷰포트 컬링 (Viewport Culling)**:
   * 현재 브라우저 화면(Viewport)에 보이는 좌표 범위 내의 노드와 라인만 렌더링하고, 화면 밖에 있는 노드는 가상 돔(Virtual DOM)에서 제외하여 렌더링 오버헤드를 90% 차단합니다.
3. **데이터 최적화**:
   * Zustand에서 500개 노드의 활성화 여부를 매번 객체로 복사하지 않고, 활성화된 노드 ID 리스트를 `Set` 또는 `String Bitmask`로 가볍게 관리하여 상태 업데이트 쿨타임을 1ms 이하로 제한합니다.
